describe('UserResolver', () => {
  beforeEach(() => {});

  describe('fetchUser', () => {
    it('유저정보 꺼내오기', () => {
      // controller와 같은 방식으로 테스트 하기
    });
  });
  describe('createUser', () => {
    it('유저정보 등록히기', () => {
      // controller와 같은 방식으로 테스트 하기
    });
  });
});
